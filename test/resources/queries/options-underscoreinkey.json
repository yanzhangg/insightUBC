{
	"title": "invalid query with underscore in COLUMNS key",
	"input": {
		"WHERE": {
			"GT": {
				"sections_fail": 200
			}
		},
		"OPTIONS": {
			"COLUMNS": [
				"sect_ions_dept",
				"sections_fail"
			]
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
